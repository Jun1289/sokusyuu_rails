databases:
  - name: taskleaf_production
    plan: free
    # ipAllowList: []
    databaseName: taskleaf_production
    user: Rails_deploy_test
    region: singapore

services:
  - type: web
    name: Rails_deploy_test
    env: ruby
    region: singapore
    plan: free
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: taskleaf_production
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false

